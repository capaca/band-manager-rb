<h1><%= t('band_manager.site.session.concerts.title') %></h1>

<%= 
  render :partial => 'concerts_list', :locals => {
    :band => @band, :concerts => @concerts
  }
%>

<%= will_paginate @concerts %>

<div class="voltar" >
  <%= link_to t('band_manager.site.back'), 
      { :controller => :site, :action => :index, :locale => params[:locale] }, 
      :class => 'all back_icon' %>
</div>


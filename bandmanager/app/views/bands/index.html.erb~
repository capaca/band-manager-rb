<h1>Bandas</h1>



<table class="listagem">
  <tr>
    <th>Name</th>
    <th>Genre</th>
    <th>Year</th>
    <th>City</th>
    <th>Country</th>
    <th>About</th>
    <th>Editar</th>
    <th>Apagar</th>        
  </tr>

<% @bands.each_with_index do |band, index| %>
  <div class="banda">
    <h2>
      <%= link_to band.name, band %>
    </h2>
    
    <div class="info">
      <strong>Estilo:</strong> <%= band.genre.title %> 
    </div>
    
    <div class="info">
      <strong>Localização:</strong> <%= band.city %>/<%= band.country.name %>
    </div> 
    <p>
      <%= band.about %>      
    </p>
  </div>
  
  
 
    <td><%= link_to band.name, band %></td>
    <td><%=h band.genre.title %></td>
    <td><%=h band.year %></td>
    <td><%=h band.city %></td>
    <td><%=h band.country.name %></td>
    <td><%=h band.about %></td>
    <td><%= link_to 'Editar', edit_band_path(band) %></td>
    <td><%= link_to 'Apagar', band, :confirm => 'Você tem certeza?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New band', new_band_path %>
